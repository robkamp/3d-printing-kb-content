{
  "name": "3d-printing-kb-content",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Knowledge base entries for the 3D printing knowledge base, plus the contract they are validated against.",
  "license": "CC-BY-SA-4.0",
  "scripts": {
    "validate": "tsx scripts/validate.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "sweep": "tsx scripts/check-source-freshness.ts"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "gray-matter": "^4.0.3",
    "prettier": "^3.9.6",
    "tsx": "^4.23.12",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
